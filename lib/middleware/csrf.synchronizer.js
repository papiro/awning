'use strict'

module.exports = function synchronizer (req, res, done) {
  // Eventually this will generate and check synchronizer tokens
  //  But for now it'll check the existence of a custom header being set by
  //    the client.
  done()
}
